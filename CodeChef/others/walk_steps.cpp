#include <bits/stdc++.h>
using namespace std;      
#define endl '\n'
 
#define pb(x) push_bacK(x)
#define forf(i,n) for(int i=0;i<(int)(n);i++)
#define fore(i,a,b) for(int i=(int)(a);(a>=b)?i>=b:i<=b;(a>=b)?i-=1:i+=1)
#define forb(i,n) for(int i=(int)(n-1);i>=0;i--)
typedef long long int int64;
typedef vector<int> vint;
typedef vector<int64> vint64;
const long MAX = 1e9+7;

int main()
{
	ios_base::sync_with_stdio(0); cin.tie(NULL);
	freopen("in.txt", "r", stdin);
	freopen("out.txt", "w", stdout);


	// didn't understand the problem  

}


Taxable Value -> Taxable Value (₹)
Integrated Tax Amount -> Tax Amount Integrated Tax  (₹)
Central Tax Amount -> Tax Amount Central Tax (₹)
State Tax Amount -> Tax Amount State/UT tax (₹)
